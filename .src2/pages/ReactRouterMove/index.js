import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ReactRouterMove = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default ReactRouterMove;

const styles = StyleSheet.create({});
